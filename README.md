# pp

pp is a simple printer for Golang.

```
import "github.com/hlcfan/pp"

m := map[string]string{"foo": "bar", "hello": "world"}
pp.Print(m)
```

  map[string]string {
    foo: 	bar,
    hello: 	world,
  }

